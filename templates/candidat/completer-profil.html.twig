{% extends 'base.html.twig' %}

{% block title %}Candidat{% endblock %}

{% block body %}
<div class="container">
    <div class="mt-5">
    {% for message in app.flashes('error') %}
<div class="alert alert-danger">
    {{ message }}
</div>
    {% endfor %}

    <div class="mt-5">
<h1 class="text-center">completer mon profil</h1>
    </div>

    {{ form_start(form) }}
        {{ form_row(form.nom) }}
        {{ form_row(form.prenom) }}
        {{ form_row(form.cvFile) }}
        {{ form_row(form.cvName) }}
        {{ form_row(form.Enregistrer) }}
    {{ form_end(form) }}
</div>
</div>
{% endblock %}